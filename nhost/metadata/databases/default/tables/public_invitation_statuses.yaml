table:
  name: invitation_statuses
  schema: public
is_enum: true
array_relationships:
- name: invitations
  using:
    foreign_key_constraint_on:
      column: status
      table:
        name: invitations
        schema: public
